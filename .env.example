# docker compose 用 —— 复制为 .env 后按需填写。
# （仅 docker-compose.yml 读取；各服务自身的 .env 在 packages/<service>/ 下。）

# Postgres 超级用户密码
POSTGRES_PASSWORD=password

# api-gateway 管理后台密码（留空 = 本地开发不校验）
ADMIN_PASSWORD=

# agent-gateway 的 LLM key（OpenRouter）；留空则 Agent 对话功能不可用
OPENROUTER_API_KEY=
